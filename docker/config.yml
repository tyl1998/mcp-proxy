# mcp-proxy 默认配置文件（用于 Docker 环境）

server:
  # 监听端口
  port: 8089
  # 监听地址
  host: 0.0.0.0

log:
  # 日志级别: trace, debug, info, warn, error
  level: info
  # 日志文件路径（Docker 环境下输出到 stdout，此配置仅用于本地文件日志）
  path: /app/logs
  # 保留最近 N 个日志文件
  retain_days: 3
